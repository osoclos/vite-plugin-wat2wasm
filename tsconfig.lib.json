{
    "extends": "./tsconfig.json",

    "include": ["src/index.ts"],
    "exclude": ["dist"],

    "compilerOptions": {
        // Emit
        "declaration": true,
        "emitDeclarationOnly": true,
        "noEmit": false,
        "outDir": "dist",

        // Language and Environment
        "lib": ["ES2023", "DOM"],
        "target": "ES2023"
    }
}
